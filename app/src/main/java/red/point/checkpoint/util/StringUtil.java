package red.point.checkpoint.util;

public class StringUtil {

    public static String[] split(String text, String separator) {
        String[] separated = text.split(separator);
        return separated;
    }
}
